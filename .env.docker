# Docker/PostgreSQL Configuration
# Use this for Docker-based development:
#   cp .env.docker .env && docker compose up -d

DATABASE_URL=postgresql://postgres:postgres@db:5432/sherlock
RUN_DB_MIGRATIONS_ON_STARTUP=true

ZENROWS_API_KEY=3f05e02f2fcd9d0de313b60efb0687ca58bf8189

# Data Ingestion Configuration
INGESTION_INTERVAL_HOURS=6
ENABLE_AUTO_INGESTION=true

# Data Ingestion Limits (Medium pull configuration)
MAX_PAGES=25
MAX_DETAIL_CALLS=200

# Location Configuration - City-wide search
# Let the intelligence pipeline filter and rank
SEARCH_LOCATION=san-francisco-ca

# Search Filter Configuration
# Wider net - scoring system handles quality filtering
SEARCH_PRICE_MIN=600000
SEARCH_BEDS_MIN=1
SEARCH_BATHS_MIN=1
SEARCH_SQFT_MIN=500
